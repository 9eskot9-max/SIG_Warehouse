[project]
name = "sig_warehouse"
description = "SIG Warehouse Workbench - ERP-native dispatch, returns and custody for ERPNext"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.frappe-dependencies]
frappe = ">=15.0.0-dev,<16.0.0"
